// Math.ceil() : 소수점 올림, 정수 반환
// Math.floor() : 소수점 버림, 정수 반환
// Math.round() : 소수점 반올림, 정수 반환

function solution(flo) {
  return Math.floor(flo);
}

let result = solution(1.42);
console.log(result);
